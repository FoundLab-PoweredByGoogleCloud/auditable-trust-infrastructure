/* ===========================================================
   FoundLab · ATI Whitepaper — Editorial kit (WeasyPrint 69)
   Cormorant Garamond / Inter / JetBrains Mono
   Navy #14213D · Bronze #A88965
   =========================================================== */

@font-face { font-family:'Cormorant'; src:url('file:///home/claude/fonts/CormorantGaramond.ttf'); font-weight:300 700; font-style:normal; }
@font-face { font-family:'Cormorant'; src:url('file:///home/claude/fonts/CormorantGaramond-Italic.ttf'); font-weight:300 700; font-style:italic; }
@font-face { font-family:'Inter'; src:url('file:///home/claude/fonts/Inter.ttf'); font-weight:100 900; font-style:normal; }
@font-face { font-family:'Inter'; src:url('file:///home/claude/fonts/Inter-Italic.ttf'); font-weight:100 900; font-style:italic; }
@font-face { font-family:'JBMono'; src:url('file:///home/claude/fonts/JetBrainsMono.ttf'); font-weight:100 800; font-style:normal; }
@font-face { font-family:'JBMono'; src:url('file:///home/claude/fonts/JetBrainsMono-Italic.ttf'); font-weight:100 800; font-style:italic; }

:root{
  --navy:#14213D; --navy-deep:#0d1730; --bronze:#A88965; --bronze-dk:#8f6f4d;
  --ink:#1c1c1e; --muted:#6f6a61; --faint:#928c82;
  --hair:#d8d2c6; --hair-lt:#e8e3d9; --panel:#f6f3ed; --panel-2:#efeae1;
  --paper:#ffffff; --code-bg:#0f1a33; --code-ink:#dfe5f1; --code-dim:#8da0c4;
  --sans:'Inter','DejaVu Sans',sans-serif;
  --serif:'Cormorant','DejaVu Serif',serif;
  --mono:'JBMono','DejaVu Sans Mono',monospace;
}

/* ---------- PAGE GEOMETRY ---------- */
@page{
  size:A4; margin:20mm 19mm 18mm 19mm;
  @top-left{ content:"FOUNDLAB · AUDITABLE TRUST INFRASTRUCTURE"; font-family:var(--mono); font-size:6pt; letter-spacing:.14em; color:var(--faint); }
  @top-right{ content:string(secrun); font-family:var(--mono); font-size:6pt; letter-spacing:.12em; color:var(--faint); }
  @bottom-center{ content:counter(page); font-family:var(--mono); font-size:7pt; letter-spacing:.1em; color:var(--muted); }
  @bottom-right{ content:"ATI · v1.0"; font-family:var(--mono); font-size:6pt; letter-spacing:.12em; color:var(--faint); }
}
@page:first{ @top-left{content:none;} @top-right{content:none;} @bottom-center{content:none;} @bottom-right{content:none;} }
@page cover{ margin:0; background:var(--navy); @top-left{content:none;} @top-right{content:none;} @bottom-center{content:none;} @bottom-right{content:none;} }
@page opener{ margin:0; background:var(--navy); @top-left{content:none;} @top-right{content:none;} @bottom-center{content:none;} @bottom-right{content:none;} }
@page back{ margin:0; background:var(--navy); @top-left{content:none;} @top-right{content:none;} @bottom-center{content:none;} @bottom-right{content:none;} }

/* ---------- BASE ---------- */
*{ box-sizing:border-box; }
html{ font-family:var(--sans); font-size:9.7pt; line-height:1.56; color:var(--ink); }
body{ margin:0; hyphens:auto; -weasy-hyphens:auto; text-rendering:optimizeLegibility; }
p{ margin:0 0 7pt 0; text-align:justify; }
strong{ font-weight:650; color:#111; }
em{ font-style:italic; }
sup,sub{ font-size:.72em; line-height:0; }
a{ color:inherit; text-decoration:none; }

/* ---------- COVER ---------- */
.cover{ page:cover; color:#eef1f7; padding:24mm 22mm 20mm 22mm; height:297mm; break-after:page; position:relative; overflow:hidden; }
.cover::after{ content:""; position:absolute; left:22mm; right:22mm; top:21mm; height:1px; background:linear-gradient(90deg,var(--bronze),rgba(168,137,101,0)); }
.cv-kick{ font-family:var(--mono); font-size:7.5pt; letter-spacing:.34em; color:var(--bronze); text-transform:uppercase; }
.cv-title{ font-family:var(--serif); font-weight:600; font-size:33pt; line-height:1.06; letter-spacing:.002em; margin:14mm 0 0 0; color:#fff; }
.cv-title .lo{ color:#c7d0e2; }
.cv-sub{ font-family:var(--serif); font-style:italic; font-size:14.5pt; color:#aeb9d0; margin:6mm 0 0 0; font-weight:400; }
.cv-brand{ font-family:var(--mono); font-size:8pt; letter-spacing:.18em; color:#dfe5f1; margin-top:5mm; }
.cv-brand b{ color:var(--bronze); font-weight:600; }
.cv-loc{ font-family:var(--mono); font-size:7pt; letter-spacing:.2em; color:#7d88a3; margin-top:1.5mm; text-transform:uppercase; }
.cv-abstract{ margin-top:13mm; border-left:2px solid var(--bronze); padding:1mm 0 1mm 6mm; max-width:150mm; }
.cv-abstract .lab{ font-family:var(--mono); font-size:7pt; letter-spacing:.24em; color:var(--bronze); text-transform:uppercase; display:block; margin-bottom:3mm; }
.cv-abstract p{ font-size:9.6pt; line-height:1.6; color:#d4dae8; text-align:justify; margin:0; }
.cv-badges{ margin-top:9mm; display:flex; flex-wrap:wrap; gap:4pt; }
.cv-badges span{ font-family:var(--mono); font-size:6.4pt; letter-spacing:.14em; color:#c7d0e2; border:1px solid rgba(168,137,101,.5); padding:3pt 6pt; text-transform:uppercase; }
.cv-foot{ position:absolute; left:22mm; right:22mm; bottom:18mm; display:flex; justify-content:space-between; gap:14mm; }
.cv-foot .blk{ flex:1; }
.cv-foot .lab{ font-family:var(--mono); font-size:6.4pt; letter-spacing:.2em; color:var(--bronze); text-transform:uppercase; display:block; margin-bottom:2.5mm; }
.cv-foot .val{ font-family:var(--sans); font-size:8.4pt; color:#d4dae8; line-height:1.4; }

/* ---------- SECTION OPENER ---------- */
.opener{ page:opener; break-before:page; break-after:page; color:#eef1f7; height:297mm; padding:32mm 22mm 22mm 22mm; position:relative; overflow:hidden; }
.opener .op-num{ font-family:var(--serif); font-size:120pt; line-height:.8; font-weight:500; color:rgba(168,137,101,.92); letter-spacing:-.02em; }
.opener .op-kick{ font-family:var(--mono); font-size:8pt; letter-spacing:.3em; color:#8794b0; text-transform:uppercase; margin-top:10mm; }
.opener .op-title{ font-family:var(--serif); font-weight:600; font-size:30pt; line-height:1.08; color:#fff; margin:5mm 0 0 0; max-width:150mm; }
.opener .op-sub{ font-family:var(--serif); font-style:italic; font-size:14pt; color:#aeb9d0; margin-top:5mm; }
.opener .op-abs{ margin-top:14mm; max-width:152mm; }
.opener .op-abs .lab{ font-family:var(--mono); font-size:7pt; letter-spacing:.24em; color:var(--bronze); text-transform:uppercase; display:block; margin-bottom:3mm; }
.opener .op-abs p{ font-size:9.4pt; line-height:1.62; color:#cfd6e6; text-align:justify; }
.opener .op-toc{ position:absolute; left:22mm; bottom:22mm; right:22mm; display:flex; flex-wrap:wrap; gap:3mm 9mm; border-top:1px solid rgba(168,137,101,.35); padding-top:6mm; }
.opener .op-toc span{ font-family:var(--mono); font-size:7.2pt; letter-spacing:.06em; color:#aab4ca; }
.opener .op-toc span b{ color:var(--bronze); font-weight:600; }

/* ---------- HEADINGS / RHYTHM ---------- */
.section-title{ string-set:secrun content(text); }   /* feeds running header */
h2.h-sec{ font-family:var(--serif); font-size:21pt; font-weight:600; color:var(--navy); margin:0 0 1mm 0; line-height:1.12; }
.kicker{ font-family:var(--mono); font-size:7pt; letter-spacing:.2em; text-transform:uppercase; color:var(--bronze-dk); margin:0 0 2.5mm 0; }
h3.h-sub{ break-before:auto; margin:9mm 0 0 0; }
h3.h-sub .lede{ font-family:var(--serif); font-size:18.5pt; font-weight:600; color:var(--navy); line-height:1.16; display:block; }
.standfirst{ font-family:var(--sans); font-size:9.6pt; font-style:italic; color:var(--muted); margin:2.5mm 0 4mm 0; line-height:1.5; text-align:left; }
.rule{ height:1px; background:var(--hair); border:0; margin:5mm 0; }
.rule-bronze{ height:2px; width:34mm; background:var(--bronze); border:0; margin:0 0 5mm 0; }

/* two-column body where useful */
.cols2{ column-count:2; column-gap:8mm; column-rule:1px solid var(--hair-lt); }
.cols2 p{ margin-bottom:6pt; }
.nobreak{ break-inside:avoid; }

/* ---------- EQUATION ---------- */
.eq{ position:relative; margin:5mm 0; padding:4mm 14mm 4mm 6mm; background:var(--panel); break-inside:avoid; }
.eq .ex{ font-family:var(--mono); font-size:9.3pt; color:var(--navy); display:block; text-align:center; line-height:1.5; }
.eq .eqno{ position:absolute; right:6mm; top:50%; transform:translateY(-50%); font-family:var(--serif); font-style:italic; font-size:11pt; color:var(--bronze-dk); }
.eq.tight{ padding:3mm 14mm 3mm 6mm; }
.eq .note{ font-family:var(--sans); font-size:8pt; color:var(--muted); display:block; text-align:center; margin-top:2mm; font-style:italic; }

/* ---------- DEFINITION / CALLOUT ---------- */
.defbox{ background:var(--panel); padding:5mm 6mm; margin:5mm 0; break-inside:avoid; }
.defbox .lab{ font-family:var(--mono); font-size:7pt; letter-spacing:.18em; color:var(--bronze-dk); text-transform:uppercase; display:block; margin-bottom:2.5mm; }
.defbox .def{ font-family:var(--serif); font-size:13pt; line-height:1.4; color:var(--navy); font-weight:500; text-align:left; }

.invariant{ border:1px solid var(--hair); padding:4mm 6mm; margin:5mm 0; break-inside:avoid; background:#fff; }
.invariant .lab{ font-family:var(--mono); font-size:7pt; letter-spacing:.18em; color:var(--bronze-dk); text-transform:uppercase; }
.invariant p{ margin:2mm 0 0 0; font-size:9.2pt; color:#33312d; }

/* impact cards — "E daí, pra mim?" */
.impact{ display:flex; gap:5mm; margin:5mm 0; break-inside:avoid; }
.impact .card{ flex:1; background:var(--panel); padding:4.5mm 5mm; }
.impact .card .who{ font-family:var(--mono); font-size:7pt; letter-spacing:.16em; text-transform:uppercase; color:var(--bronze-dk); display:block; margin-bottom:2mm; }
.impact .card .ti{ font-family:var(--serif); font-size:12.5pt; font-weight:600; color:var(--navy); display:block; margin-bottom:2mm; line-height:1.15; }
.impact .card p{ font-size:8.6pt; line-height:1.5; margin:0; color:#3a3833; text-align:left; }

/* three-requirement / generic triad cards */
.triad{ display:flex; gap:4mm; margin:5mm 0; break-inside:avoid; }
.triad .t{ flex:1; background:#fff; border:1px solid var(--hair); padding:4mm 4.5mm; }
.triad .t .n{ font-family:var(--mono); font-size:7pt; letter-spacing:.14em; color:var(--bronze-dk); display:block; }
.triad .t .h{ font-family:var(--serif); font-size:12.5pt; font-weight:600; color:var(--navy); display:block; margin:1mm 0 2mm 0; line-height:1.15; }
.triad .t p{ font-size:8.3pt; line-height:1.46; margin:0; color:#3a3833; text-align:left; }

/* ---------- CODE ---------- */
.code-wrap{ margin:5mm 0; break-inside:avoid; }
.code-cap{ font-family:var(--mono); font-size:6.8pt; letter-spacing:.12em; text-transform:uppercase; color:var(--muted); margin-bottom:1.5mm; }
pre.code{ background:var(--code-bg); color:var(--code-ink); font-family:var(--mono); font-size:7.4pt; line-height:1.5; padding:4mm 5mm; margin:0; white-space:pre-wrap; word-break:break-word; }
pre.code .c{ color:var(--code-dim); }
pre.code .k{ color:#9ec5ff; }
pre.code .s{ color:#c2e0a8; }
pre.code .kw{ color:#e0a0d0; }
code{ font-family:var(--mono); font-size:.86em; color:var(--navy); background:var(--panel-2); padding:.3pt 2pt; }

/* ---------- TABLES ---------- */
table.tbl{ width:100%; border-collapse:collapse; margin:5mm 0; font-size:8.3pt; break-inside:avoid; }
table.tbl caption{ caption-side:top; text-align:left; font-family:var(--mono); font-size:6.8pt; letter-spacing:.12em; text-transform:uppercase; color:var(--muted); margin-bottom:2mm; }
table.tbl th{ background:var(--navy); color:#eef1f7; font-family:var(--sans); font-weight:600; font-size:7.2pt; letter-spacing:.04em; text-transform:uppercase; text-align:left; padding:2.6mm 3mm; }
table.tbl td{ padding:2.6mm 3mm; border-bottom:1px solid var(--hair-lt); vertical-align:top; line-height:1.4; }
table.tbl tr:nth-child(even) td{ background:#faf8f4; }
table.tbl td.mono,table.tbl th.mono{ font-family:var(--mono); font-size:7.6pt; }
table.tbl td.num{ text-align:right; font-family:var(--mono); font-variant-numeric:tabular-nums; }
table.tbl .ok{ color:#3c7a4e; font-weight:700; }
.tbl-note{ font-family:var(--mono); font-size:7pt; color:var(--muted); margin-top:-2mm; line-height:1.5; }

/* stat callouts */
.stats{ display:flex; gap:4mm; margin:5mm 0; break-inside:avoid; }
.stats .s{ flex:1; background:var(--navy); color:#fff; padding:4mm; text-align:center; }
.stats .s .big{ font-family:var(--serif); font-size:26pt; font-weight:600; line-height:1; color:#fff; }
.stats .s .lab{ font-family:var(--mono); font-size:6.6pt; letter-spacing:.12em; text-transform:uppercase; color:var(--bronze); margin-top:2mm; display:block; }
.stats .s .sub{ font-size:7.4pt; color:#c7d0e2; margin-top:1mm; display:block; }

/* figure / console (web-interactive rendered static) */
.figure{ border:1px solid var(--hair); background:#fff; margin:5mm 0; break-inside:avoid; }
.figure .fhd{ background:var(--panel); padding:2.5mm 4mm; font-family:var(--mono); font-size:7pt; letter-spacing:.12em; text-transform:uppercase; color:var(--bronze-dk); border-bottom:1px solid var(--hair); }
.figure .fbody{ padding:4mm 5mm; }
.figcap{ font-family:var(--sans); font-style:italic; font-size:7.8pt; color:var(--muted); margin-top:2mm; line-height:1.45; }
.statusbar{ font-family:var(--mono); font-size:6.8pt; letter-spacing:.06em; color:#cfd6e6; background:var(--navy); padding:2mm 4mm; }
.statusbar b{ color:var(--bronze); }

.pipeline{ display:flex; gap:2mm; margin:3mm 0; }
.pipeline .step{ flex:1; text-align:center; font-family:var(--mono); font-size:6.8pt; color:var(--navy); background:var(--panel); padding:2.5mm 1mm; letter-spacing:.04em; }

/* numbered process list */
ol.proc{ counter-reset:p; list-style:none; padding:0; margin:4mm 0; }
ol.proc li{ position:relative; padding:0 0 4mm 11mm; break-inside:avoid; }
ol.proc li::before{ counter-increment:p; content:"0" counter(p); position:absolute; left:0; top:0; font-family:var(--mono); font-size:8pt; font-weight:700; color:var(--bronze-dk); letter-spacing:.04em; }
ol.proc li b{ color:var(--navy); }
ol.proc li .t{ font-size:8.8pt; line-height:1.5; color:#3a3833; }

/* regulatory anchor cards */
.regrow{ display:flex; gap:4mm; margin:5mm 0; break-inside:avoid; }
.regrow .r{ flex:1; border-top:2px solid var(--bronze); background:var(--panel); padding:3.5mm 4mm; }
.regrow .r .n{ font-family:var(--mono); font-size:8.5pt; font-weight:700; color:var(--navy); letter-spacing:.02em; }
.regrow .r p{ font-size:8pt; line-height:1.45; margin:2mm 0; text-align:left; color:#3a3833; }
.regrow .r .st{ font-family:var(--mono); font-size:6.6pt; letter-spacing:.14em; text-transform:uppercase; color:var(--bronze-dk); }

/* maturity columns */
.mat{ display:flex; gap:4mm; margin:5mm 0; break-inside:avoid; }
.mat .col{ flex:1; }
.mat .col .ch{ font-family:var(--mono); font-size:7pt; letter-spacing:.12em; text-transform:uppercase; padding:2mm 3mm; color:#fff; }
.mat .p .ch{ background:#2f5d43; } .mat .d .ch{ background:#8a6d3b; } .mat .r .ch{ background:#52617e; }
.mat .col ul{ list-style:none; margin:0; padding:0; }
.mat .col li{ border:1px solid var(--hair-lt); border-top:0; padding:2.4mm 3mm; font-size:7.8pt; line-height:1.35; }
.mat .col li b{ display:block; color:var(--navy); font-size:8.2pt; }
.mat .col li .v{ font-family:var(--mono); font-size:6.4pt; color:var(--bronze-dk); float:right; }
.mat .col li small{ color:var(--muted); }

/* verification note */
.vnote{ border-top:1px solid var(--hair); margin-top:5mm; padding-top:2.5mm; font-family:var(--mono); font-size:7pt; line-height:1.55; color:var(--muted); }
.vnote b{ color:var(--bronze-dk); }

/* pull line */
.pull{ font-family:var(--serif); font-style:italic; font-size:13.5pt; line-height:1.35; color:var(--navy); text-align:center; margin:6mm 4mm; }

/* ---------- TOC ---------- */
.toc h2{ font-family:var(--serif); font-size:24pt; font-weight:600; color:var(--navy); margin:0 0 1mm 0; }
.toc .tk{ font-family:var(--mono); font-size:7pt; letter-spacing:.2em; text-transform:uppercase; color:var(--bronze-dk); margin-bottom:3mm; }
.toc .grp{ break-inside:avoid; margin-bottom:0.8mm; }
.toc .lvl1{ display:flex; align-items:baseline; gap:3mm; margin:0.9mm 0 .8mm 0; }
.toc .lvl1 .rn{ font-family:var(--serif); font-style:italic; font-size:13pt; color:var(--bronze-dk); width:12mm; }
.toc .lvl1 .tt{ font-family:var(--serif); font-size:14pt; font-weight:600; color:var(--navy); flex:1; }
.toc .lvl1 .pg{ font-family:var(--mono); font-size:8pt; color:var(--navy); }
.toc .lvl2{ display:flex; align-items:baseline; margin:.45mm 0 .45mm 15mm; font-size:8.6pt; color:#43403a; }
.toc .lvl2 .tt{ flex:1; }
.toc .lvl2 .dots{ flex:0 1 auto; border-bottom:1px dotted var(--hair); margin:0 2mm 1.2mm 2mm; min-width:6mm; }
.toc .lvl2 .pg{ font-family:var(--mono); font-size:7.6pt; color:var(--muted); }

/* ---------- EXEC SUMMARY ---------- */
.exec .big-claim{ font-family:var(--serif); font-size:23pt; font-weight:600; color:var(--navy); line-height:1.12; margin:3mm 0 5mm 0; }

/* ---------- BIBLIOGRAPHY ---------- */
.biblio .ref{ display:flex; gap:4mm; margin-bottom:4mm; break-inside:avoid; font-size:8.6pt; line-height:1.5; }
.biblio .ref .id{ font-family:var(--mono); font-size:8pt; color:var(--bronze-dk); width:8mm; flex:none; }
.biblio .ref .bd{ flex:1; color:#3a3833; }
.biblio .ref .bd b{ color:var(--navy); }

/* ---------- BACK COVER ---------- */
.back{ page:back; break-before:page; color:#eef1f7; height:297mm; padding:0; position:relative; overflow:hidden; }
.back .wrap{ position:absolute; left:22mm; right:22mm; top:50%; transform:translateY(-55%); }
.back .bk{ font-family:var(--mono); font-size:7.5pt; letter-spacing:.3em; color:var(--bronze); text-transform:uppercase; }
.back .ti{ font-family:var(--serif); font-size:46pt; font-weight:600; color:#fff; line-height:1.02; margin:6mm 0 0 0; }
.back .ds{ font-family:var(--serif); font-style:italic; font-size:15pt; color:#aeb9d0; margin-top:6mm; max-width:130mm; line-height:1.4; }
.back .brand{ font-family:var(--mono); font-size:9pt; letter-spacing:.26em; color:#dfe5f1; margin-top:14mm; }
.back .brand b{ color:var(--bronze); }
.back::after{ content:""; position:absolute; left:22mm; width:34mm; height:2px; background:var(--bronze); top:50%; margin-top:-34mm; }

/* utility */
.mono{ font-family:var(--mono); }
.small{ font-size:8pt; }
.muted{ color:var(--muted); }
.spacer{ height:4mm; }
.avoid{ break-inside:avoid; }

/* ---------- BOOKMARKS + RUNNING HEADER FEED ---------- */
.runtag{ string-set:secrun content(text); display:block; height:0; line-height:0; overflow:hidden; font-size:0; color:transparent; }
.opener .op-title{ bookmark-level:1; bookmark-label:content(text); }
h2.h-sec{ bookmark-level:1; bookmark-label:content(text); }
h3.h-sub{ bookmark-level:2; bookmark-label:content(text); }
